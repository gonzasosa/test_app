name: book_repository
description: Package providing a repository for fetching data for books
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.1 <3.0.0"

dependencies:
  api_client:
    path: ../api_client
  equatable: ^2.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  flutter_lints: ^2.0.0
